# Shared data available on all machines (committed to repo).
# Per-machine overrides go in ~/.config/chezmoi/chezmoi.toml [data] section.

# LAPRAS専用 GPG 鍵の Key ID (long format, 16桁)
# 空の場合: lapras リポジトリでも global の signingkey が使われる
# 設定後: lapras-inc リポジトリで自動的にこの鍵で署名される
gpg_lapras = "B8D4D1B7D959D552"
